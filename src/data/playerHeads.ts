interface SkinData {
    bust: string;
    head: string;
    ign: string;
}

interface MCApiResponse {
    cache: object;
    id: string;
    name: string;
    status: string;
}

export type { SkinData, MCApiResponse };
